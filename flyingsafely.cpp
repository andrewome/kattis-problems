#include <bits/stdc++.h>
using namespace std;

int main() {
  int TC;
  cin >> TC;
  while (TC--) {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
      int a, b;
      cin >> a >> b;
    }
    cout << n-1 << endl;
  }
  return 0;
}